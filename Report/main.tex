%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CN-Project - Course Report
% Complex Networks, Spring 2026
% Università di Bologna
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[9pt,a4paper,twocolumn,twoside]{rho-class/rho}
\usepackage[english]{babel}
\usepackage{needspace}

% ===== Uniform publication spacing ==========================================
% Display equations: identical glue above and below every formula. The class
% sets these to a rigid, slightly negative value inside \normalsize, so we
% re-patch \normalsize (ours runs last and wins). Crucially the two "short"
% skips are set equal to the regular ones, so a formula's spacing does not
% depend on whether the line before it was full or short. A small symmetric
% rubber lets flushbottom balance columns without opening paragraph gaps.
\makeatletter
\expandafter\def\expandafter\normalsize\expandafter{%
    \normalsize%
    \setlength\abovedisplayskip{6pt plus 2pt minus 1pt}%
    \setlength\belowdisplayskip{6pt plus 2pt minus 1pt}%
    \setlength\abovedisplayshortskip{6pt plus 2pt minus 1pt}%
    \setlength\belowdisplayshortskip{6pt plus 2pt minus 1pt}%
}
\makeatother

% Floats: a single separation value for in-text [H] floats (\intextsep),
% top/bottom [t] floats (\textfloatsep), stacked floats (\floatsep) and the
% full-width figure* floats (\dbltextfloatsep, \dblfloatsep). Every figure and
% table therefore sits the same distance from the surrounding text.
\setlength\floatsep{14pt plus 2pt minus 2pt}
\setlength\textfloatsep{14pt plus 2pt minus 2pt}
\setlength\intextsep{14pt plus 2pt minus 2pt}
\setlength\dblfloatsep{14pt plus 2pt minus 2pt}
\setlength\dbltextfloatsep{14pt plus 2pt minus 2pt}

% Allow a tall full-width figure* (the RA--Dec map) to sit at the top of a page
% with body text flowing beneath it, instead of being banished to a dedicated
% float page that would leave the rest of the page blank.
\renewcommand{\dbltopfraction}{0.92}
\renewcommand{\dblfloatpagefraction}{0.7}
\renewcommand{\topfraction}{0.9}
\renewcommand{\textfraction}{0.08}

% Captions: identical gap between every caption and its figure/table body,
% whether the caption sits above (tables) or below (figures).
% The class sets justification=centering; we override it completely in
% \AtBeginDocument so it wins after every package hook (including ragged2e).
\captionsetup[table]{skip=7pt}
\AtBeginDocument{%
    \captionsetup[figure]{format=plain, labelsep=period,
        labelfont={small,bf,sf}, textfont={small},
        justification=raggedright, singlelinecheck=false, skip=7pt}%
    \captionsetup[figure*]{format=plain, labelsep=period,
        labelfont={small,bf,sf}, textfont={small},
        justification=raggedright, singlelinecheck=false, skip=7pt}%
}
% ============================================================================

% Bibliography: small font and tight entry spacing to keep all refs on one page.
% Override the class's sorting=ynt with citation order (IEEE standard): references
% are numbered in order of first appearance in the text.
\ExecuteBibliographyOptions{sorting=none}
\renewcommand{\bibfont}{\footnotesize}
\setlength\bibitemsep{1pt plus 1pt minus 1pt}

% The class forces tabular content into Fira Sans (\sffamily), whose geometric
% digits clash with the serif STIX figures used in the body and inline math.
% Append a hook (runs after the class's) to switch table content to the roman
% face, so every number shares one academic, lining-figure style.
\AtBeginEnvironment{tabular}{\rmfamily}

% Graceful figure fallback: shows a placeholder box if the PDF has not been
% generated yet (i.e. the notebook has not been run). Once the figure PDFs
% exist in figures/, they replace the boxes automatically on the next compile.
\newcommand{\placefig}[2][width=\columnwidth]{%
    \IfFileExists{#2}%
        {\includegraphics[#1]{#2}}%
        {\fbox{\rule{0pt}{4.5cm}\rule{\columnwidth}{0pt}}}%
}

%----------------------------------------------------------
% Title
%----------------------------------------------------------

\doctype{Project Report: Complex Networks}
\title{Reproducing the Tempel et al. (2014) SDSS DR10 Galaxy Group Catalog with an Unsupervised Cylindrical FoF Driven by Network Percolation}

%----------------------------------------------------------
% Authors and affiliations
%----------------------------------------------------------

\author[{\textasteriskcentered},a]{Ali Tabaeian}

\affil[a]{Dipartimento di Fisica \& Astronomia, Università di Bologna}

\dates{Spring, 2026}

%----------------------------------------------------------
% Corresponding author and document info
%---------------------------------------------------------- 

\corres{\textsuperscript{\textasteriskcentered}\href{mailto:mohamadali.tabaeian@studio.unibo.it}{mohamadali.tabaeian@studio.unibo.it}}

\docinfo{Course project for Complex Networks, Spring 2026. Code: \href{https://github.com/M-A-Tbn/CN-project}{github.com/M-A-Tbn/CN-project}.}

%----------------------------------------------------------

\journalname{Complex Networks}
\journal{Università di Bologna, 2026}
\theday{\ifcase\month\or January\or February\or March\or April\or May\or June\or July\or August\or September\or October\or November\or December\fi~\the\year}


%----------------------------------------------------------
% Abstract
%----------------------------------------------------------

\begin{abstract}
We identify galaxy groups in the SDSS DR10 Main Galaxy Sample using a cylindrical
Friends-of-Friends algorithm, treating it explicitly as a random geometric graph
in comoving space. We derive the linking length from the network's own
percolation behaviour rather than calibrating it externally: the susceptibility peaks
at $r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc, while the finite-group count peaks
at the sub-critical scale $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc, which we take
as the group-finding scale. Compared against the Tempel et al.\ catalogue as a
graph-partition problem, the groups score ARI $= 0.82$ and NMI $= 0.98$
($p < 0.001$ against a permutation null). The asymmetry (high recall, lower
precision) points to mild over-merging in the richest systems. The network is
itself far from random: clustering four orders of magnitude above the
Erd\H{o}s--R\'enyi baseline, and a degree distribution heavy-tailed at both ends.
\end{abstract}

\keywords{galaxy groups, Friends-of-Friends, network percolation, SDSS, large-scale structure, complex networks}

%----------------------------------------------------------

\begin{document}

\maketitle
\thispagestyle{firststyle}
% Override the default header (full title overflows in two-column layout)
\fancyhead[RO,LE]{\footerfont Cylindrical FoF Galaxy Groups via Network Percolation}

%----------------------------------------------------------

\section{Introduction}

\rhostart{G}alaxy groups, gravitationally bound systems of a few to a hundred
members, are the environment of most galaxies and the building blocks of the
cosmic web \cite{tempel2014}. Identifying them from a redshift survey is, at
heart, a graph problem: place an edge between every pair of galaxies closer than a
chosen linking length, and read the groups off as the connected components. This
Friends-of-Friends (FoF) prescription \cite{huchra1982, davis1985} maps a
physical criterion, spatial proximity, directly onto a graph-theoretic
object, but it leaves one parameter undetermined: the linking length itself,
conventionally written as the dimensionless $b = \ell/\bar d$. Traditionally $b$
has been fixed by calibration against $N$-body simulations \cite{berlind2002}
or adopted wholesale from an earlier survey \cite{tempel2014}.

This work asks whether the galaxy network can supply that scale itself. We treat
the FoF construction as a random geometric graph and study how its connectivity
changes as the linking length is varied. Such a graph undergoes a percolation
transition: below a critical linking length the network consists of small finite
clusters; above it a single giant component spans the volume.
Two scales emerge from the transition, defined
purely topologically. The susceptibility, the second moment of the finite-cluster
size distribution, peaks at the percolation threshold $r_{\mathrm{critical}}$,
above which only superclusters and filaments survive. The number of distinct
finite groups peaks at a smaller scale $r_{\mathrm{groups}}$, where the network is
fragmented into the largest number of bound systems before they begin merging into
the giant component. We adopt $r_{\mathrm{groups}}$ as the operating point and
require $r_{\mathrm{groups}} < r_{\mathrm{critical}}$.

Because the line-of-sight coordinate is reconstructed from redshift, peculiar
velocities stretch groups into radial filaments (the Fingers-of-God effect),
so the linking volume must be an anisotropic cylinder rather than a sphere
\cite{tempel2014}. The scale $r_{\mathrm{groups}}$ and the group
catalogue it produces then follow from the network topology alone.

We apply the method to the SDSS DR10 Main Galaxy Sample and validate it against
the group catalogue of Tempel et al.\ \cite{tempel2014}. That catalogue has a
dual role: it provides the galaxy data, and its group assignments are the
ground truth against which our partition is measured. The linking length, however,
is read only from the network, never from Tempel's groups. The comparison is
framed throughout as a graph-partition problem, and the network is also examined
for its topological properties as a complex-network object.

\section{Data}

Deriving the linking length from network topology only works if the underlying
point set is unbiased: a distance-dependent selection would shift the percolation
scales by construction, so the sample below is cut to be volume-limited.

\subsection{Source catalog}

The galaxy sample is drawn from the catalogue of Tempel et al.\
\cite{tempel2014}, retrieved through the VizieR service. The underlying data are
the spectroscopic galaxies of the Sloan Digital Sky Survey Data Release 10 (SDSS
DR10) Main Galaxy Sample (MGS) \cite{ahn2014}. A spectroscopic survey measures
each galaxy's redshift from the Doppler shift of identified spectral lines;
this gives a precise line-of-sight distance, in contrast to a photometric
survey, which estimates redshift from broad-band colours with far larger
uncertainty. That precision makes a redshift-space group finder viable:
the radial coordinate is reliable enough that the only serious distortion is the
peculiar-velocity (Fingers-of-God) effect addressed in the next section. The MGS
is magnitude-limited, complete to an apparent $r$-band magnitude
$m_{\mathrm{lim}} = 17.77$. From the catalogue we retain the position, redshift
and magnitude of each galaxy; the group identifiers assigned by Tempel et al.\
are held back and used only as the validation ground truth below.

\subsection{Cosmology}

Redshifts are converted to comoving distances under a flat $\Lambda$CDM cosmology
with $H_0 = 100\,h\,\mathrm{km\,s^{-1}\,Mpc^{-1}}$ and $\Omega_m = 0.27$. The
comoving distance to redshift $z$ is
\begin{equation}\label{eq:comoving}
    d_C(z) = \frac{c}{H_0}\int_0^z
    \frac{\mathrm{d}z'}{\sqrt{\Omega_m(1+z')^3 + \Omega_\Lambda}},
\end{equation}
with $\Omega_\Lambda = 1 - \Omega_m$. Setting $H_0 = 100\,h$ expresses every
distance in $h^{-1}$ Mpc and removes the residual uncertainty in the Hubble
constant from the analysis. The matter density $\Omega_m$ fixes the shape of the
distance--redshift relation and hence the physical scale assigned to a given
angular and redshift separation. Both parameters are chosen to match the
cosmology adopted by Tempel et al.\ \cite{tempel2014}: because the linking length
is a physical distance, any mismatch in cosmology would rescale it relative to
the reference catalogue and render the validation below unfair.

\subsection{Volume-limited sample}

The MGS is flux-limited, so at increasing distance only progressively brighter
galaxies remain above $m_{\mathrm{lim}}$; a raw redshift slice would therefore
sample systematically more luminous galaxies at its far edge and bias the
inferred group population with distance. To remove this bias we construct a
volume-limited sample. We first restrict to the redshift interval $0.02 \le z \le
0.06$: the lower bound suppresses the large fractional peculiar-velocity errors
of the very nearby universe, while the upper bound keeps the volume compact enough
that a single fixed linking length remains appropriate throughout. We then impose
the absolute-magnitude limit corresponding to $m_{\mathrm{lim}}$ at the far edge
of the volume,
\begin{equation}\label{eq:Mlim}
    M_{\mathrm{lim}} = m_{\mathrm{lim}} - \mu(z_{\max}),
\end{equation}
where $\mu(z)$ is the distance modulus, and keep only galaxies with $M \le
M_{\mathrm{lim}}$. The sample is then complete to the same absolute magnitude
throughout the volume. These cuts reduce the parent catalogue from $588{,}193$ to
$75{,}042$ galaxies.

\subsection{Coordinate conversion}

The network is built in physical space, so each galaxy is mapped from its
observed $(d_C, \alpha, \delta)$ (comoving distance, right ascension,
declination) to Cartesian coordinates,
\begin{equation}\label{eq:cartesian}
    X = d_C\cos\delta\cos\alpha, \quad
    Y = d_C\cos\delta\sin\alpha, \quad
    Z = d_C\sin\delta,
\end{equation}
with the observer at the origin. This embedding places every galaxy in the metric
space a random geometric graph requires, and supplies the line-of-sight direction
for the pair-separation decomposition. The next section builds the graph on this
point set and reads its connectivity.

\section{Method}

\subsection{Cylindrical linking criterion}

The Friends-of-Friends procedure constructs a graph $G(r_\perp)$ on the set of
$N$ galaxies: each galaxy is a node, and an edge is drawn between every pair
whose separation falls below a chosen linking length. The connected components
of $G$ are identified as galaxy groups. This is precisely the construction of a
\emph{random geometric graph} (RGG): the nodes carry
positions in a metric space (here the comoving Cartesian coordinates of each
galaxy), and adjacency is determined solely by proximity in that space. Adjacency
is therefore fixed by geometry, not by any astrophysical assumption.

A spherical linking length is, however, inappropriate in redshift space. The
line-of-sight coordinate is reconstructed from the observed redshift, which
mixes the cosmological recession with the peculiar velocity of each galaxy.
Within a virialised system, peculiar velocities of $300$--$500\,\mathrm{km\,
s^{-1}}$ displace galaxies by several $h^{-1}$ Mpc along the line of sight, while
the true transverse extent of the same system is only $\sim 0.5\,h^{-1}$ Mpc.
Groups are thereby stretched into radial filaments pointing at the observer,
the \emph{Fingers-of-God} effect \cite{jackson1972}. A spherical criterion large
enough to absorb this elongation would over-link in the transverse direction and
merge physically distinct systems.

The usual fix is an anisotropic, cylindrical linking volume
\cite{tempel2014}. For a pair $(i,j)$ with comoving positions
$\vec r_i, \vec r_j$, the separation is decomposed along the line of sight. With
the observer at the origin, the local line-of-sight direction at the pair
midpoint is $\hat{\ell}_{ij} = (\vec r_i + \vec r_j)/|\vec r_i + \vec r_j|$, and
the parallel and perpendicular separations are
\begin{equation}\label{eq:decomp}
    d_\parallel = \bigl| (\vec r_i - \vec r_j)\cdot \hat{\ell}_{ij}\bigr|,
    \qquad
    d_\perp = \sqrt{\,|\vec r_i - \vec r_j|^2 - d_\parallel^2\,}.
\end{equation}
The pair is linked when it lies inside a cylinder aligned with the line of sight,
\begin{equation}\label{eq:cylinder}
    d_\perp \le r_\perp
    \quad\text{and}\quad
    d_\parallel \le r_\parallel = S\,r_\perp,
\end{equation}
where $r_\perp$ is the transverse linking length and $S$ the dimensionless
stretch factor. Following Tempel et al.\ \cite{tempel2014} we fix $S = 10$; this
is a shape parameter set by the typical ratio of radial to transverse
distortion, and it is the only parameter taken from the literature.
The scale $r_\perp$ is left free and is fixed entirely by the topological
analysis below. Candidate pairs are found with a $k$-d tree: a
single range query returns every pair inside the bounding sphere of radius
$r_{\max} = \sqrt{r_\perp^2 + r_\parallel^2}$ that encloses the cylinder, after
which \equref{eq:cylinder} selects the true links.
\figref{fig:cylinder} illustrates the two linking volumes.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.78\columnwidth]{figures/imp.pdf}
    \caption{Cylindrical FoF linking criterion, normalized to $r_\perp = 1$.
    The sky-blue circle is the isotropic FoF boundary; the dashed vermilion
    rectangle is the cylindrical boundary elongated by factor $S$ along the
    line of sight. In the analysis $r_\perp = 0.54\,h^{-1}$ Mpc and $S = 10$;
    the schematic uses $S = 2.5$ for legibility.}
    \label{fig:cylinder}
\end{figure}

\subsection{Percolation scan}

The transverse linking length is the single free parameter of the construction,
and the structure of the graph changes qualitatively as it is varied. We scan
$r_\perp$ over $[0.1, 2.5)\,h^{-1}$ Mpc in steps of $0.02\,h^{-1}$ Mpc,
rebuilding $G(r_\perp)$ and extracting its connected components at each step, and
we monitor two complementary topological observables.

The first is the \emph{susceptibility},
\begin{equation}\label{eq:chi}
    \chi(r_\perp) = \frac{1}{N}\sum_{s\,\neq\,\mathrm{GCC}} s^2\, n_s,
\end{equation}
where $n_s$ is the number of components containing $s$ galaxies and the sum runs
over every component except the giant connected component (GCC). The
susceptibility is proportional to the second moment of the finite-cluster size
distribution; in the thermodynamic limit it diverges at the percolation
threshold, and for a finite system it peaks there. We denote this peak location $r_{\mathrm{critical}}$. It
is the scale at which a single component first spans the survey volume: above it
the analysis recovers filaments and superclusters rather than groups.

The second observable is the \emph{fragmentation count},
\begin{equation}\label{eq:nfrag}
    N_{\mathrm{groups}}(r_\perp)
    = \bigl|\{\,c : 2 \le |c| < |\mathrm{GCC}|\,\}\bigr|,
\end{equation}
the number of components that are neither isolated nodes nor the GCC. This
counts the genuine groups present at a given linking length. It is maximised at
the scale where the network is broken into the largest number of distinct,
finite systems, immediately before they coalesce into the percolating component;
we identify this scale, $r_{\mathrm{groups}}$, as the operating point of the
group finder. Both $r_{\mathrm{critical}}$ and $r_{\mathrm{groups}}$ are read off
the network's own topology, with no reference to the Tempel group assignments.

\subsection{GCC diagnostic}

\figref{fig:gcc} shows both scales directly. The upper panel tracks the
fraction of galaxies in the GCC as $r_\perp$ increases; the lower panel shows
its derivative. Three independent
markers of the transition are overlaid. The susceptibility peak places
$r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc, and the point of steepest GCC growth
(the maximum of $\mathrm{d}\,\mathrm{GCC}/\mathrm{d}r_\perp$) gives
$1.36\,h^{-1}$ Mpc, in close agreement; the two methods bracket the same
percolation threshold. The fragmentation maximum sits well below, at
$r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc. The defining requirement of the method,
\begin{equation}\label{eq:subcritical}
    r_{\mathrm{groups}} < r_{\mathrm{critical}},
\end{equation}
is therefore satisfied: the group-finding scale is comfortably sub-critical, and
the catalogue produced at $r_{\mathrm{groups}}$ describes bound groups rather
than the percolating cosmic web.

\begin{figure}[H]
    \centering
    \placefig{figures/GCC_Diagnostic_Cylindrical_r_perp_Scan.pdf}
    \caption{Giant connected component (GCC) diagnostic for the cylindrical
    scan. \emph{Top}: fraction of galaxies in the GCC as a function of the
    transverse linking length $r_\perp$. \emph{Bottom}: its derivative
    $\mathrm{d}\,\mathrm{GCC}/\mathrm{d}r_\perp$. The susceptibility peak
    ($r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc) and the maximum-gradient point
    ($1.36\,h^{-1}$ Mpc) locate the percolation threshold, while the
    fragmentation maximum ($r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc) marks the
    sub-critical group-finding scale adopted in this work.}
    \label{fig:gcc}
\end{figure}

\subsection{Linking parameter}

To connect the topologically derived scale to the conventional FoF literature,
we express it as the dimensionless linking parameter
\begin{equation}\label{eq:b}
    b \equiv \frac{r_{\mathrm{groups}}}{\bar d},
    \qquad \bar d = n^{-1/3},
    \qquad n = \frac{N}{V},
\end{equation}
where $\bar d$ is the mean inter-galaxy separation and $n$ the number density of
the sample. The volume-limited slice occupies a spherical shell between the
comoving distances at $z_{\min}$ and $z_{\max}$, restricted to the survey
footprint,
\begin{equation}\label{eq:vol}
    V = \frac{4\pi}{3}\bigl(d_{\max}^3 - d_{\min}^3\bigr)\,f_{\mathrm{sky}},
    \qquad
    f_{\mathrm{sky}} = \frac{7221\,\mathrm{deg}^2}{41253\,\mathrm{deg}^2},
\end{equation}
the SDSS Main Galaxy Sample covering approximately $7221\,\mathrm{deg}^2$. This
gives $b = 0.144$. The value $b = 0.144$ lands within the classical calibrated range
$0.14 < b < 0.20$ \cite{davis1985, berlind2002}, and slightly above the
redshift-dependent Tempel et al.\ value ($b \approx 0.113$--$0.115$ over the same
interval). We compute $b$ only to locate $r_{\mathrm{groups}}$ in the conventional
literature; the linking length itself came from the topology.

\subsection{Final network}

The group catalogue is produced by building the graph once at the operating
point $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc. The resulting network contains
$N = 75{,}042$ nodes and $95{,}310$ edges. Its connected components are extracted
and each galaxy is assigned a group label; this output is exactly that of
single-linkage hierarchical clustering cut at $r_{\mathrm{groups}}$, the simplest
community-detection rule, in which two galaxies share a group whenever they are
joined by a chain of pairwise links. Unlike modularity-based methods it imposes
no resolution limit, but it is correspondingly sensitive to bridging: a single
close pair can merge two otherwise distinct systems.

\section{Results}

\subsection{The percolation transition}

\figref{fig:phase} shows both observables across the full scan. The susceptibility
peak at $r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc and the fragmentation maximum
at $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc are both sharp and well-resolved,
confirming the two scales located in \figref{fig:gcc}.

The transition is the spatial-graph counterpart of the giant-component transition
in random-graph theory. In an Erd\H{o}s--R\'enyi graph the giant component emerges
at mean degree $\langle k\rangle = 1$, that is, at edge probability $p_c = 1/N$;
in a random geometric graph it is instead governed by a critical radius,
$r_c \sim (\ln N / N)^{1/d}$ in $d$ dimensions. At our operating point the mean
degree is $\langle k\rangle = 2.54$, comfortably above the threshold at which an
equivalent random graph would develop a giant component, yet no comparable giant
component appears in the FoF network. Connectivity builds locally, assembling
groups, long before anything percolates globally; the mean-field estimate below
makes the gap quantitative.

The sharpness of the peak is itself a finite-size effect. A true divergence
occurs only in the thermodynamic limit; for a finite graph the transition is a
smooth crossover whose width narrows as $N$ grows. The
volume-limited sample of $\sim 7.5\times10^4$ galaxies, drawn from a parent
catalogue of $\sim 6\times10^5$, is large enough that the peak is well localised
and $r_{\mathrm{critical}}$ can be read off unambiguously.

The mean-field theory of percolation provides an analytic check on these
numbers. In an Erd\H{o}s--R\'enyi graph, the probability $u$ that a randomly
chosen node lies outside the giant component satisfies the self-consistency
equation
\begin{equation}\label{eq:gcc_mf}
    u = e^{\lambda(u-1)},
\end{equation}
where $\lambda = \langle k\rangle$. A nontrivial solution ($u < 1$, giant
component present) exists only for $\lambda > 1$, recovering the mean-field
threshold at $\langle k\rangle = 1$. At the operating point $\lambda = 2.54$,
the equation yields $u \approx 0.10$: a random graph of the same edge density
would place $90\%$ of its nodes in a single giant component, more than a
hundred times the $0.8\%$ observed in the FoF network. Spatial embedding in the
cosmic web suppresses the global percolation that mean-field theory predicts at
this mean degree. The FoF network clusters locally into groups that never
coalesce into a single spanning structure. Proximity in comoving space is
concentrated wherever galaxies are, so connectivity assembles bound systems
before it can percolate.

\begin{figure}[t]
    \centering
    \placefig{figures/Anisotropic_Phase_Transition_Cylindrical_r_perp.pdf}
    \caption{The anisotropic percolation transition. The susceptibility
    $\chi(r_\perp)$ (left axis) and the number of finite groups
    $N_{\mathrm{groups}}(r_\perp)$ (right axis) are shown against the transverse
    linking length. $\chi$ peaks at the percolation threshold
    $r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc (dashed), while the fragmentation
    count peaks at the sub-critical group scale $r_{\mathrm{groups}} =
    0.54\,h^{-1}$ Mpc (dotted), the operating point of the group finder.}
    \label{fig:phase}
\end{figure}

\subsection{Group catalogue: sky projection}

\figref{fig:radec} projects the Tempel et al.\ catalogue (left column) and the
detected catalogue (right column) onto the plane of the sky in four redshift bins
of width $\Delta z = 0.01$ (rows). Grouped galaxies are coloured by their group
label and sized by richness; field galaxies appear in grey. The two catalogues
are visually almost indistinguishable: the same sky overdensities are resolved
into the same groups in both columns. The number of grouped galaxies per bin tracks the underlying
galaxy count and shows no systematic growth with redshift. The fixed transverse
linking length therefore does not over-link at larger distances across the
volume-limited range.

The panel-by-panel agreement also checks the coordinate pipeline: errors in the
angular-to-Cartesian transformation or the comoving-distance integral would show
up as spatial offsets between matched groups in the two columns, and none appear.
At larger scales, prominent overdensities thread through adjacent redshift bins,
and both catalogues assign them to the same structures: the cylindrical FoF
follows extended associations without breaking them at the panel boundaries.

\begin{figure*}[p]
    \centering
    \placefig[width=\textwidth]{figures/Galaxy_distribution_and_groups_in_RA_Dec_detected_vs_Tempel_2014_per_redshift_bin.pdf}
    \caption{Sky projection (RA--Dec) of the Tempel et al.\ catalogue (left
    column) and the detected groups (right column), in four redshift bins of
    width $\Delta z = 0.01$ (rows). Grouped galaxies are coloured by group
    membership and sized by richness; field galaxies are grey.}
    \label{fig:radec}
\end{figure*}

\subsection{Group richness distribution}

\figref{fig:richness} compares the richness distributions (the number of
members per group) of the detected and Tempel catalogues, restricted to groups
of at least two members. The two agree closely over the full range, both falling
steeply with richness as expected for a hierarchical galaxy distribution. Two
systematic failure modes would be visible here: a shift of the detected
distribution toward higher richness would indicate over-merging, with small
groups incorrectly bridged into larger structures, while a shift toward lower
richness would indicate fragmentation of rich clusters by a linking cylinder too
small to span their velocity dispersion. Neither is present at the level of the
global distribution; the mild excess of rich detected groups is quantified in the
validation that follows. The steep decline shared by both catalogues reflects the
rarity of very rich systems: massive groups are the end product of hierarchical
merging, and in the volume surveyed only a small fraction of the sky has assembled
the high density contrasts needed to produce them.

\begin{figure}[t]
    \centering
    \placefig{figures/Group_Richness_Distribution.pdf}
    \caption{Group richness distribution (members per group, $N \ge 2$) for the
    detected catalogue (filled) and the Tempel et al.\ catalogue (step). The two
    agree across the full range; a systematic offset would signal over-merging
    (excess at high $N$) or fragmentation (excess at low $N$).}
    \label{fig:richness}
\end{figure}

\section{Validation \& Assessment}

Validating the catalogue against Tempel et al.\ is a \emph{graph-partition
comparison}: both catalogues partition the same set of galaxies into groups, and
the question is how closely the two partitions agree. We address it at three
levels: individual matched groups (by richness and by redshift), the global
partition, and the network topology (clustering and degree distribution). The
section then profiles the richest components individually.

\needspace{4\baselineskip}
\subsection{Completeness and purity by richness}

At the group level, for each Tempel group $T$ (with at least two members) we identify the detected
group $D$ with which it shares the most galaxies and compute
\begin{equation}\label{eq:cp}
    \mathrm{Completeness} = \frac{|T \cap D|}{|T|}, \qquad
    \mathrm{Purity} = \frac{|T \cap D|}{|D|}.
\end{equation}
The two are asymmetric: completeness is the fraction of a reference group
recovered, purity the fraction of the matched detected group that is genuine.
Binned by richness (\tabref{tab:richness}), completeness is uniformly high
($\gtrsim 0.98$) and shows no trend: essentially every member of a Tempel group
is recovered. Purity instead declines monotonically, from $0.82$ for pairs to
$0.75$ for the richest systems. The detected groups thus contain all the true
members but, in the richest systems, a few additional galaxies: the fixed stretch
factor $S = 10$ slightly under-corrects the largest velocity dispersions and
links a handful of interlopers along the line of sight. A fixed-aperture cylinder
cannot adapt to the full range of cluster dispersions, so some contamination at
the rich end is unavoidable.

\begin{table}[H]
    \caption{Mean completeness and purity of matched groups, binned by Tempel
    group richness $N_T$.}
    \label{tab:richness}
    \centering
    \begin{tabular}{lcc}
        \toprule
        Richness $N_T$ & Completeness & Purity \\
        \midrule
        $2$        & 0.995 & 0.824 \\
        $3$–$5$    & 0.992 & 0.805 \\
        $6$–$10$   & 0.984 & 0.778 \\
        $11$–$50$  & 0.984 & 0.757 \\
        $51$–$500$ & 0.989 & 0.753 \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Completeness and purity by redshift}

\figref{fig:cpz} repeats the matched-group analysis binned by mean group
redshift. Completeness remains near unity and purity fluctuates around its mean
with no strong systematic trend across the volume. The fixed linking length
therefore does not over- or under-link with distance over the volume-limited
range, consistent with the sky projection of \figref{fig:radec}.

The stability is physically expected. The transverse linking length $r_\perp$
is applied in comoving coordinates and converted to an angular separation at
each galaxy's position, so the physical scale probed is invariant throughout
the volume. The volume-limited selection at $M_{\mathrm{lim}}$ simultaneously
suppresses the Malmquist-type bias that would inflate group sizes at the far
edge of the survey and depress purity at high $z$.

\begin{figure}[t]
    \centering
    \placefig{figures/Completeness_and_Purity_by_Redshift_Bin.pdf}
    \caption{Mean completeness and purity of matched groups as a function of mean
    group redshift. Both are stable across the volume-limited survey, indicating
    that the fixed transverse linking length remains appropriate at all
    distances.}
    \label{fig:cpz}
\end{figure}

\subsection{Global partition metrics}

For the global comparison, the two partitions are evaluated on the $41{,}430$
galaxies that Tempel places in groups of at least two members ($10{,}907$ Tempel
groups, $9{,}739$ detected groups). The remaining $75{,}042 - 41{,}430 =
33{,}612$ galaxies include both our isolated nodes and Tempel's field galaxies;
both partitions agree on them trivially, so their inclusion would inflate every
score. Two standard partition-comparison scores
are reported in \tabref{tab:metrics}: the Adjusted Rand Index (ARI),
the fraction of galaxy pairs co-classified by both partitions,
corrected for chance; and the Normalized Mutual Information (NMI),
the information one partition carries about the other. Both lie on $[0,1]$, with
$0$ for chance agreement and $1$ for identity. The detected partition scores
ARI $= 0.82$ and NMI $= 0.98$.

The pair confusion matrix makes the asymmetry concrete. Pair recall, the fraction
of Tempel co-group pairs recovered, is $0.98$, while pair precision, the fraction
of our co-group pairs that are genuine, is $0.71$. At the level of pairs this
repeats the richness-analysis conclusion: the catalogue is highly complete but
mildly over-merged. We also test whether the agreement is significant. Permuting
the detected labels $1000$ times and recomputing the ARI produces a null
distribution centred at zero with standard deviation $5.7\times10^{-5}$; the
observed value lies far outside the null ($z \approx 1.4\times10^4$; $p <
0.001$).

\begin{table}[H]
    \caption{Global partition-comparison metrics between the detected and Tempel
    catalogues, on the grouped subsample ($41{,}430$ galaxies).}
    \label{tab:metrics}
    \centering
    \begin{tabular}{lc}
        \toprule
        Metric & Value \\
        \midrule
        Adjusted Rand Index (ARI)          & 0.824 \\
        Normalized Mutual Information (NMI) & 0.983 \\
        \midrule
        Pair precision                     & 0.710 \\
        Pair recall                        & 0.982 \\
        Pair $F_1$                         & 0.824 \\
        \bottomrule
    \end{tabular}
\end{table}

\needspace{6\baselineskip}
\subsection{Network topology}

At $r_{\mathrm{groups}}$ the full network has $75{,}042$ nodes and $95{,}310$
edges, fragmenting into $37{,}500$ connected components; the largest spans only $585$ nodes ($0.8\%$), confirming
the sub-critical operating point. The mean degree is $\langle k\rangle = 2.54$.
Against this sits the central topological result. The Erd\H{o}s--R\'enyi null
(a random graph with the same number of nodes and edges, hence the same edge
probability $p_{\mathrm{ER}} = 2E/[N(N-1)] = 3.4\times10^{-5}$) has an expected
clustering coefficient equal to $p_{\mathrm{ER}}$. The measured transitivity is
$0.646$ (\tabref{tab:clustering}): a galaxy's two neighbours are themselves
linked nearly two-thirds of the time, exceeding the random expectation by a
factor of $\sim 1.9\times10^4$.

This four-orders-of-magnitude excess reflects the spatially clustered structure
of the galaxy distribution. It is the same excess clustering seen in social, biological and technological
networks, but here it is driven by gravity rather than social or functional
dynamics: galaxies sharing a common neighbour are physically close, and physical
proximity is transitive.

\begin{table}[H]
    \caption{Clustering of the FoF network against the Erd\H{o}s--R\'enyi null at
    the same edge density ($p_{\mathrm{ER}} = 3.4\times10^{-5}$).}
    \label{tab:clustering}
    \centering
    \begin{tabular}{lcc}
        \toprule
        Quantity & Observed & Ratio to ER \\
        \midrule
        Transitivity          & 0.646 & $1.9\times10^4$ \\
        Mean local clustering & 0.322 & $9.5\times10^3$ \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Degree distribution}

\figref{fig:degree} shows the degree distribution $P(k)$ of the same network on
semi-logarithmic and double-logarithmic axes, with the Poisson law of the
Erd\H{o}s--R\'enyi null at the same mean degree overlaid. The distribution
departs from the null at both extremes. The isolated-node fraction is $34.9\%$
(field galaxies of degree zero), against a Poisson expectation of $7.9\%$,
an excess of $4.4\times$. At the opposite end a heavy tail reaches $k_{\max} =
43$, populated by hub galaxies whose neighbourhoods a Poisson graph of the same
mean degree would essentially never produce; the null lies tens of orders of
magnitude below the data at high $k$. Because probability is conserved, the
over-population of both extremes leaves the distribution depleted near its mean:
galaxies are either isolated in the field or embedded in a dense group, with few
in between. The hubs are the brightest central galaxies of rich systems.

The double-logarithmic panel shows the tail is not a pure power law: it is
concave and truncated at $k_{\max}$. The cutoff is geometric in origin, since
the degree is bounded by the number of galaxies that can physically occupy a
single linking cylinder. The network is therefore heavy-tailed but not scale-free,
consistent with a random geometric graph rather than a preferential-attachment
network.

\begin{figure*}[t]
    \centering
    \placefig[width=\textwidth]{figures/Degree_Distribution.pdf}
    \caption{Degree distribution $P(k)$ of the FoF network on semi-logarithmic
    (left) and double-logarithmic (right) axes. The dashed curve is the Poisson
    law of an Erd\H{o}s--R\'enyi graph of the same mean degree. The data exceed
    the null at both ends (a $34.9\%$ isolated fraction and a heavy tail to
    $k_{\max} = 43$) and the tail is concave and truncated, not a pure power
    law.}
    \label{fig:degree}
\end{figure*}

\subsection{Internal structure of the richest groups}

The global metrics average over $37{,}500$ components; \tabref{tab:groups}
profiles the eight richest detected groups individually. The eight groups show a consistent pattern: the internal edge density rises as groups shrink, from $0.02$ for the
largest to $0.08$ for the smallest tabulated, while the transitivity stays in the
range $0.54$–$0.64$. Yet even the richest groups are far from compact: their
diameters (the longest internal shortest path) reach $16$ to $36$ links
across only a few hundred members. These are extended, filamentary chains, not
clique-like balls. Single-linkage clustering produces exactly such structures, a
direct manifestation of its bridging sensitivity. The giant connected component
($585$ members, diameter $22$) is the largest such chain; group $1017$, with a
diameter of $36$ across $226$ members, is the most filamentary of all.

This geometry is the same bridging sensitivity seen in the global metrics: the
property that lets FoF trace cosmic-web filaments at large scales also lets a
single close pair fuse two systems at the group scale, the mechanism behind the
mild over-merging measured in \tabref{tab:richness} and \tabref{tab:metrics}.

\begin{table}[H]
    \caption{Internal structure of the eight richest detected groups: number of
    members $N$, internal edge density, transitivity, and diameter.}
    \label{tab:groups}
    \centering
    \begin{tabular}{rrccc}
        \toprule
        Group & $N$ & Density & Transitivity & Diameter \\
        \midrule
        133  & 585 & 0.021 & 0.561 & 22 \\
        4    & 302 & 0.038 & 0.542 & 17 \\
        0    & 301 & 0.047 & 0.579 & 18 \\
        1017 & 226 & 0.040 & 0.615 & 36 \\
        242  & 156 & 0.046 & 0.635 & 19 \\
        262  & 154 & 0.064 & 0.594 & 17 \\
        251  & 142 & 0.070 & 0.612 & 16 \\
        10   & 141 & 0.084 & 0.629 & 16 \\
        \bottomrule
    \end{tabular}
    \tabletext{Group $133$ is the giant connected component.}
\end{table}

\section{Discussion}

The main result is that the group scale can be recovered from the network
alone. The fragmentation criterion selects $r_{\mathrm{groups}} = 0.54\,h^{-1}$
Mpc, or $b = 0.144$, with no input from the reference catalogue, and the
resulting groups reproduce the Tempel partition at ARI $= 0.82$. The
topologically chosen linking length is slightly larger than Tempel's effective $b
\approx 0.113$–$0.115$, and the direction of the difference is consistent with
the validation: a marginally longer linking length explains the mild over-merging
seen as declining purity in rich systems (\tabref{tab:richness}) and as pair
precision below recall (\tabref{tab:metrics}). The fragmentation maximum favours
linking lengths just large enough to assemble groups fully, at the cost of
occasionally bridging close pairs, the characteristic weakness of single
linkage.

The percolation scan also converts a free parameter into a measured one. The
susceptibility and fragmentation curves give two physically motivated scales:
$r_{\mathrm{critical}}$, above which group finding recovers filaments rather than
groups, and $r_{\mathrm{groups}}$, at which the group population is largest. The
sub-critical inequality $r_{\mathrm{groups}} < r_{\mathrm{critical}}$ is an
internal consistency check that any FoF catalogue should satisfy.

The method has known failure modes. Single-linkage clustering has no resolution
limit but is sensitive to bridging: a single close pair merges two groups and
produces the extended, filamentary components of \tabref{tab:groups} and the
over-merging in rich systems. The fixed stretch factor $S = 10$ cannot match the
full range of cluster velocity dispersions and under-corrects the most massive
systems. A single transverse linking length is held across the full volume;
although the redshift-binned metrics show this is adequate over $0.02 \le z \le
0.06$, it would not extrapolate to a deeper sample without a redshift-dependent
length. The survey footprint also truncates groups at its boundary, an edge
effect not corrected here.

More broadly, the percolation scan offers a physically motivated alternative to
N-body calibration: the proximity graph encodes the scale at which matter passes
from isolated nodes to bound structures, the same scale simulations target through
the halo mass function. That the topology-derived $b = 0.144$ lands in the
classical calibrated range $0.14$--$0.20$ \cite{davis1985, berlind2002} suggests
the percolation transition is a real observable of clustering, not a
methodological artefact.

\section{Conclusion}

The percolation transition does two things. It converts a free parameter, the
linking length, into a topological observable, and it provides a built-in
consistency check: any group-finding scale must sit below $r_{\mathrm{critical}}$
or the catalogue describes filaments rather than groups. On the SDSS DR10 sample
the resulting catalogue scores ARI $= 0.82$ and NMI $= 0.98$ against Tempel et
al., with the residual disagreement (mild over-merging in the richest systems)
traceable to the fixed stretch factor rather than the percolation criterion itself.
The agreement with simulation-calibrated values of $b$ was not assumed; it followed
from the network's own structure, which suggests the percolation threshold tracks
a real property of the clustering scale, independent of the method.

%----------------------------------------------------------

\printbibliography

%----------------------------------------------------------

\end{document}

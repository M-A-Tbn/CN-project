%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CN-Project - Course Report
% Complex Networks, Spring 2026
% Università di Bologna
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[9pt,a4paper,twocolumn,twoside]{rho-class/rho}
\usepackage[english]{babel}
\usepackage{needspace}

% ===== Uniform publication spacing ==========================================
% Display equations: identical glue above and below every formula. The class
% sets these to a rigid, slightly negative value inside \normalsize, so we
% re-patch \normalsize (ours runs last and wins). Crucially the two "short"
% skips are set equal to the regular ones, so a formula's spacing does not
% depend on whether the line before it was full or short. A small symmetric
% rubber lets flushbottom balance columns without opening paragraph gaps.
\makeatletter
\expandafter\def\expandafter\normalsize\expandafter{%
    \normalsize%
    \setlength\abovedisplayskip{6pt plus 2pt minus 1pt}%
    \setlength\belowdisplayskip{6pt plus 2pt minus 1pt}%
    \setlength\abovedisplayshortskip{6pt plus 2pt minus 1pt}%
    \setlength\belowdisplayshortskip{6pt plus 2pt minus 1pt}%
}
\makeatother

% Floats: a single separation value for in-text [H] floats (\intextsep),
% top/bottom [t] floats (\textfloatsep), stacked floats (\floatsep) and the
% full-width figure* floats (\dbltextfloatsep, \dblfloatsep). Every figure and
% table therefore sits the same distance from the surrounding text.
\setlength\floatsep{14pt plus 2pt minus 2pt}
\setlength\textfloatsep{14pt plus 2pt minus 2pt}
\setlength\intextsep{14pt plus 2pt minus 2pt}
\setlength\dblfloatsep{14pt plus 2pt minus 2pt}
\setlength\dbltextfloatsep{14pt plus 2pt minus 2pt}

% Allow a tall full-width figure* (the RA--Dec map) to sit at the top of a page
% with body text flowing beneath it, instead of being banished to a dedicated
% float page that would leave the rest of the page blank.
\renewcommand{\dbltopfraction}{0.92}
\renewcommand{\dblfloatpagefraction}{0.7}
\renewcommand{\topfraction}{0.9}
\renewcommand{\textfraction}{0.08}

% Captions: identical gap between every caption and its figure/table body,
% whether the caption sits above (tables) or below (figures).
% The class sets justification=centering; we override it completely in
% \AtBeginDocument so it wins after every package hook (including ragged2e).
\captionsetup[table]{skip=7pt}
\AtBeginDocument{%
    \captionsetup[figure]{format=plain, labelsep=period,
        labelfont={small,bf,sf}, textfont={small},
        justification=raggedright, singlelinecheck=false, skip=7pt}%
    \captionsetup[figure*]{format=plain, labelsep=period,
        labelfont={small,bf,sf}, textfont={small},
        justification=raggedright, singlelinecheck=false, skip=7pt}%
}
% ============================================================================

% Bibliography: small font and tight entry spacing to keep all refs on one page.
% Override the class's sorting=ynt with citation order (IEEE standard): references
% are numbered in order of first appearance in the text.
\ExecuteBibliographyOptions{sorting=none}
\renewcommand{\bibfont}{\footnotesize}
\setlength\bibitemsep{1pt plus 1pt minus 1pt}

% The class forces tabular content into Fira Sans (\sffamily), whose geometric
% digits clash with the serif STIX figures used in the body and inline math.
% Append a hook (runs after the class's) to switch table content to the roman
% face, so every number shares one academic, lining-figure style.
\AtBeginEnvironment{tabular}{\rmfamily}

% Graceful figure fallback: shows a placeholder box if the PDF has not been
% generated yet (i.e. the notebook has not been run). Once the figure PDFs
% exist in figures/, they replace the boxes automatically on the next compile.
\newcommand{\placefig}[2][width=\columnwidth]{%
    \IfFileExists{#2}%
        {\includegraphics[#1]{#2}}%
        {\fbox{\rule{0pt}{4.5cm}\rule{\columnwidth}{0pt}}}%
}

%----------------------------------------------------------
% Title
%----------------------------------------------------------

\doctype{Project Report: Complex Networks}
\title{Reproducing the Tempel et al. (2014) SDSS DR10 Galaxy Group Catalog with an Unsupervised Cylindrical FoF Driven by Network Percolation}

%----------------------------------------------------------
% Authors and affiliations
%----------------------------------------------------------

\author[{\textasteriskcentered},a]{Ali Tabaeian}

\affil[a]{Dipartimento di Fisica \& Astronomia, Università di Bologna}

\dates{Spring, 2026}

%----------------------------------------------------------
% Corresponding author and document info
%---------------------------------------------------------- 

\corres{\textsuperscript{\textasteriskcentered}\href{mailto:mohamadali.tabaeian@studio.unibo.it}{mohamadali.tabaeian@studio.unibo.it}}

\docinfo{Submitted as the final project for the Complex Networks Course, Under the supervision of Prof. Daniel Remondini and Dott. Francesco Durazzi. The LaTeX source and Jupyter Notebook containing the code are available at : \href{https://github.com/M-A-Tbn/CN-project}{github.com/M-A-Tbn/CN-project}.}

%----------------------------------------------------------

\journalname{Complex Networks}
\journal{Università di Bologna, 2026}
\theday{\ifcase\month\or January\or February\or March\or April\or May\or June\or July\or August\or September\or October\or November\or December\fi~\the\year}


%----------------------------------------------------------
% Abstract
%----------------------------------------------------------

\begin{abstract}
We identify galaxy groups in the SDSS DR10 Main Galaxy Sample using a cylindrical
Friends-of-Friends algorithm, treating it explicitly as a random geometric graph
in comoving space. We derive the linking length from the network's own
percolation behaviour rather than calibrating it externally: the susceptibility peaks
at $r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc, while the finite-group count peaks
at the sub-critical scale $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc, which we take
as the group-finding scale. Compared against the Tempel et al.\ catalogue as a
graph-partition problem, the groups score ARI $= 0.82$ and NMI $= 0.98$
(significant against a permutation null; $z \approx 1.4\times10^4$). The asymmetry
(high recall, lower precision) points to over-merging concentrated in the richest systems. The network is
itself far from random: clustering four orders of magnitude above the
Erdhos Renyi baseline, and a degree distribution heavy-tailed at both ends.
\end{abstract}

\keywords{galaxy groups, Friends-of-Friends, network percolation, SDSS, large-scale structure, complex networks}

%----------------------------------------------------------

\begin{document}

\maketitle
\thispagestyle{firststyle}
% Override the default header (full title overflows in two-column layout)
\fancyhead[RO,LE]{\footerfont Cylindrical FoF Galaxy Groups via Network Percolation}

%----------------------------------------------------------

\section{Introduction}

\rhostart{M}ost galaxies live in groups: gravitationally bound systems of
anywhere from a few members to a hundred. These groups are the building blocks
of the cosmic web \cite{tempel2014}. Identifying them from a redshift survey is,
at heart, a graph problem: place an edge between every pair of galaxies closer
than a chosen linking length, and read the groups off as the connected
components. This Friends-of-Friends (FoF) prescription \cite{huchra1982,
davis1985} maps a physical criterion, spatial proximity, directly onto a
graph-theoretic object, but it leaves one parameter undetermined: the linking
length itself, conventionally written as the dimensionless $b = \ell/\bar d$.
Traditionally $b$ has been fixed by calibration against $N$-body simulations
\cite{berlind2002} or adopted wholesale from an earlier survey \cite{tempel2014}.

This work asks whether the galaxy network can supply that scale itself. We treat
the FoF construction as a random geometric graph and study how its connectivity
changes as the linking length is varied. Such a graph undergoes a percolation
transition. Keep the linking length short and the network is just small,
disconnected clusters. Push past a critical value and they suddenly merge into
one giant component that spans the whole volume. Two scales come out of the
transition. The susceptibility is the second moment of the finite-cluster size
distribution. It peaks right at the threshold $r_{\mathrm{critical}}$; beyond
that point, only superclusters and filaments are left. At a shorter scale,
$r_{\mathrm{groups}}$, the number of distinct finite groups reaches its maximum,
just before inter-group bridges begin to dominate and components merge into the
giant. We adopt $r_{\mathrm{groups}}$ as the operating point and require
$r_{\mathrm{groups}} < r_{\mathrm{critical}}$.

Because the line-of-sight coordinate is reconstructed from redshift, peculiar
velocities stretch groups into radial filaments (the Fingers-of-God effect),
so the linking volume must be an anisotropic cylinder rather than a sphere
\cite{tempel2014}. Both scales are read off the topology; no external
calibration sets $r_{\mathrm{groups}}$.

We apply the method to the SDSS DR10 Main Galaxy Sample and validate it against
the group catalogue of Tempel et al.\ \cite{tempel2014}. We use that catalogue
twice over. It gives us the galaxy data, and its own group assignments are what
we test our partition against. The linking length is read only from the network,
never from Tempel's groups; the comparison is framed as a graph-partition
problem throughout.

\section{Data}

Deriving the linking length solely from network topology works only if the
sample is unbiased. A distance-dependent selection would cause a bias, changing
the entire shape of the dataset and the network; therefore the sample is cut to
be volume-limited. The following changes are applied to correct for the
observational and cosmological effects.

\subsection{Source catalogue}

The galaxy sample is drawn from the catalogue of Tempel et al.\
\cite{tempel2014}, retrieved through the VizieR service. the catalogue contains
the spectroscopic galaxies of the Sloan Digital Sky Survey Data Release 10
(SDSS DR10) Main Galaxy Sample (MGS) \cite{ahn2014}. Spectroscopic redshifts
are precise and the line-of-sight distortions are due to peculiar-velocity
stretching, not measurement noise, and that is why using a redshift-space group
finder is a solid choice. The MGS is magnitude-limited, with an apparent
$r$-band magnitude limit of $m_{\mathrm{lim}} = 17.77$. We store the position,
redshift, magnitude and the group labels for each galaxy, with the latter serving
only as the validation ground truth.

\subsection{Cosmology}

To make the dataset compatible with the source catalogue and build the graph in
physical space, redshifts are converted to comoving distances under a flat
$\Lambda$CDM cosmology with $H_0 = 100\,h\,\mathrm{km\,s^{-1}\,Mpc^{-1}}$ and
$\Omega_m = 0.27$. The comoving distance to redshift $z$ is
\begin{equation}\label{eq:comoving}
    d_C(z) = \frac{c}{H_0}\int_0^z
    \frac{\mathrm{d}z'}{\sqrt{\Omega_m(1+z')^3 + \Omega_\Lambda}},
\end{equation}
with $\Omega_\Lambda = 1 - \Omega_m$. Setting $H_0 = 100\,h$ puts every
distance in units of $h^{-1}$ Mpc, which absorbs the uncertainty in the Hubble
constant. We use the same parameters as Tempel et al.\ \cite{tempel2014} because
a different cosmology would change the scale of all physical distances and leave
the validation comparison meaningless.

\subsection{Volume-limited sample}

The MGS is flux-limited. As the distance increases, only brighter galaxies stay
brighter than $m_{\mathrm{lim}}$, so a raw redshift slice would bias the group
population toward the brighter systems at the distant edge of the slice. We
restrict to $0.02 \le z \le 0.06$. This redshift range is compact enough to
ensure that we don't need to take the linking length's dependence on redshift
into account. The lower bound keeps $z$ above the threshold where peculiar
velocities dominate the redshift. We then impose the absolute-magnitude limit
corresponding to $m_{\mathrm{lim}}$ at the far edge,
\begin{equation}\label{eq:Mlim}
    M_{\mathrm{lim}} = m_{\mathrm{lim}} - \mu(z_{\max}),
\end{equation}
where $\mu(z)$ is the distance modulus, and keep only galaxies with $M \le
M_{\mathrm{lim}}$. This way, a noticeable number of galaxies are removed from
the sample but the sample becomes uniformly complete and comparable, which is an
acceptable tradeoff. No K-correction or extinction correction is applied; at the
low redshifts considered ($z \le 0.06$) these corrections are small, so the
incompleteness they introduce is treated as a known limitation, not a source of
systematic bias. These cuts reduce the population of the parent catalogue from
$588{,}193$ to $75{,}042$ galaxies.

\subsection{Coordinate conversion}

Each galaxy is mapped from its observed spherical coordinates
$(d_C, \alpha, \delta)$ to Cartesian coordinates,
\begin{equation}\label{eq:cartesian}
    X = d_C\cos\delta\cos\alpha, \quad
    Y = d_C\cos\delta\sin\alpha, \quad
    Z = d_C\sin\delta,
\end{equation}
with the observer at the origin. The Cartesian conversion is needed both for the
graph construction and for vector decomposition into transverse and line-of-sight
components.

\section{Method}

\subsection{Cylindrical linking criterion}

The Friends-of-Friends algorithm constructs a graph $G(r_\perp)$ on the set of
$N$ galaxies where each galaxy is a node, and an edge is drawn between every
pair if their distance falls inside a sphere with radius equal to the linking
length. With such criterion no group finding algorithm is needed as the connected
components of $G$ are identified as galaxy groups. This is the construction of a
\emph{random geometric graph} (RGG), built on the clustered galaxy distribution
rather than a uniform set of points. In such a graph, the position of each node
is described by the comoving Cartesian coordinates of the galaxy, and only the
proximity in that space determines adjacency. The group memberships assigned by
this method are derived entirely from the network structure, with no reference to
any external labelling. In that sense, we can claim that this procedure is
unsupervised. This must be separated from the corrections or adjustments made to
account for observational effects or cosmological assumptions dictated by
redshift-space geometry, which are necessary to ensure that the data represents
a physically meaningful structure. No group assignments from Tempel or any other
catalogue are used as input; those catalogues serve only as verification after
the fact.

The main challenge imposed by redshift-space geometry is the inappropriate use
of a spherical linking length. The line-of-sight coordinate is reconstructed
from the observed redshift, which is affected by both cosmological recession and
the peculiar velocity of each galaxy. Within a virialised system, peculiar
velocities of $300$--$500\,\mathrm{km\,s^{-1}}$ displace galaxies by several
$h^{-1}$ Mpc along the line of sight while the actual transverse range for the
same system is only $\sim 0.5\,h^{-1}$ Mpc. Due to this effect, groups are
stretched into radial filaments pointing back at the observer, creating what is
called the \emph{Fingers-of-God} effect \cite{jackson1972}. A spherical
criterion that is large enough to absorb this stretching would over-link in the
transverse direction and merge physically distinct systems into groups which they
don't belong to.

To fix this issue, an anisotropic, cylindrical linking volume is used
\cite{tempel2014}. For a pair $(i,j)$ with comoving positions
$\vec r_i, \vec r_j$, the separation is decomposed along the line of sight. With
the observer at the origin, the local line-of-sight direction at the pair
midpoint is $\hat{\ell}_{ij} = (\vec r_i + \vec r_j)/|\vec r_i + \vec r_j|$,
and the parallel and perpendicular separations are
\begin{equation}\label{eq:decomp}
    d_\parallel = \bigl|(\vec r_i - \vec r_j)\cdot\hat{\ell}_{ij}\bigr|,
    \qquad
    d_\perp = \sqrt{\,|\vec r_i - \vec r_j|^2 - d_\parallel^2\,}.
\end{equation}
The pair is only linked if it lies inside the cylinder aligned with the line of
sight,
\begin{equation}\label{eq:cylinder}
    d_\perp \le r_\perp
    \quad\text{and}\quad
    d_\parallel \le r_\parallel = S\,r_\perp,
\end{equation}
where $r_\perp$ is the transverse linking length and $S$ is the dimensionless
stretch factor. Following Tempel et al.\ \cite{tempel2014} we fix $S = 10$;
this is a shape parameter set by the typical ratio of radial to transverse
distortion, and it is the only parameter taken from the literature. In a strict
treatment $S$ is not a constant but a function of redshift. as the redshift
increases the peculiar velocities become larger, so the line-of-sight distortion
grows and the ratio of radial to transverse stretching also changes with $z$. We
keep $S$ fixed over the whole sample to keep the analysis simpler. If one
chooses to treat $S$ as a free parameter, they can perform a 2D scan for both
$r_\perp$ and $r_\parallel$ to find the optimal values. That would be a more
general approach, avoided in this work to reduce the computational complexity
and keep the analysis simpler. This leaves $r_\perp$ the only free parameter
which is fixed entirely by the topological analysis below. Candidate pairs are
found with a $k$-d tree: a single range query returns every pair inside the
bounding sphere of radius $r_{\max} = \sqrt{r_\perp^2 + r_\parallel^2}$ that
encloses the cylinder, filtered by \equref{eq:cylinder} that selects the true
links. \figref{fig:cylinder} illustrates the two linking volumes.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.78\columnwidth]{figures/imp.pdf}
    \caption{Cylindrical FoF linking criterion, normalized to $r_\perp = 1$. The
    blue circle is the isotropic FoF boundary; the dashed rectangle in green is
    the cylindrical boundary stretched by factor $S = 2.5$ (used for legibility)
    along the line of sight. In the analysis $r_{\mathrm{groups}} =
    0.54\,h^{-1}$ Mpc and $S = 10$;}
    \label{fig:cylinder}
\end{figure}

\subsection{Percolation scan}

With the transverse linking length being the single free parameter of the
construction, the structure of the graph changes qualitatively as it is varied.
We scan $r_\perp$ over $[0.1, 2.5)\,h^{-1}$ Mpc in steps of
$0.02\,h^{-1}$ Mpc. The lower bound sits just above the radius of a cD galaxy,
the largest galaxies known, whose extended stellar envelopes reach
${\sim}100\,\mathrm{kpc}$; below this the linking length would be smaller than a
single galaxy and no physically meaningful pair can be linked. It is also well
below the expected group scale (${\sim}0.5\,h^{-1}$ Mpc). The upper bound on
the other hand was found by trial and error, it sits above
$r_{\mathrm{critical}}$, far enough to confirm that the GCC is fully
established, as any value above that will just add unnecessary computation
workload with no benefit. The step size of $0.02\,h^{-1}$ Mpc gives a
resolution precise enough to locate both peaks but also maintains a reasonable
computational time. The graph $G(r_\perp)$ is rebuilt and its connected
components are extracted at each step so we can monitor two observables that
describe the percolation of the network:

The first observable is the \emph{susceptibility},
\begin{equation}\label{eq:chi}
    \chi(r_\perp) = \frac{1}{N}\sum_{s\,\neq\,\mathrm{GCC}} s^2\, n_s,
\end{equation}
where $n_s$ is the number of components containing $s$ galaxies. The sum runs
over every component except the giant connected component (GCC). For an infinite
system, this quantity goes to infinity at the percolation threshold, but since
our network is finite, it reaches a maximum and we take the location of that
peak as an estimate of the percolation threshold, denoted as
$r_{\mathrm{critical}}$. It is the scale at which a single component first
spans the sampling volume. Above this scale, the analysis recovers galaxy
clusters, filaments and superclusters rather than groups that we are interested
in.

The second observable is the \emph{fragmentation count},
\begin{equation}\label{eq:nfrag}
    N_{\mathrm{groups}}(r_\perp)
    = \bigl|\{\,c : 2 \le |c| < |\mathrm{GCC}|\,\}\bigr|,
\end{equation}

This counts every component that is neither a single galaxy nor the GCC. Galaxy
groups sit below the percolation threshold. Being the smallest structures formed
by the galaxies, they are invisible to the susceptibility peak, yet physically
real. A linking length that is too short leaves them as isolated singletons and
one that is too long over-links them into the percolating component. At an
intermediate scale, the network fragments into the largest number of distinct
systems, and that maximum is the criterion used to select $r_{\mathrm{groups}}$,
without relying on any external catalogue. Tempel's catalogue enters only
afterwards, as a check on whether this maximum fragmentation was able to
identify groups that correspond to physically recognised structures.

\subsection{GCC analysis}

A common approach to find the linking length is to analyse the giant connected
component (GCC) directly, for example by taking the maximum gradient of its
size with respect to $r_\perp$. This locates the percolation threshold, but
this is not the scale we need. \figref{fig:gcc} shows both scales in the same
frame. The upper panel tracks the size of the GCC, i.e.\ the fraction of
galaxies it contains, as $r_\perp$ increases; the lower panel shows its
derivative with respect to $r_\perp$. Three independent indicators of the
transition are marked in the plot. The susceptibility peak locates
$r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc. The maximum-gradient point gives
$1.36\,h^{-1}$ Mpc, in close agreement. Here the main challenge appears. The
susceptibility peak and the maximum-gradient point both sit near
$1.35\,h^{-1}$ Mpc which is already a cluster-scale separation, much larger
than the size of a galaxy group. For this reason a second observable, the
fragmentation count, becomes important. Its maximum sits at a much smaller
scale, $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc. The defining requirement of
the method,
\begin{equation}\label{eq:subcritical}
    r_{\mathrm{groups}} < r_{\mathrm{critical}},
\end{equation}
is therefore satisfied: the group-finding scale is well below the percolation
threshold. $r_{\mathrm{critical}}$ is not used to find groups; it is computed to
confirm that $r_{\mathrm{groups}}$ sits in the sub-critical regime where the
network still resolves distinct finite structures.

\begin{figure}[H]
    \centering
    \placefig{figures/GCC_Diagnostic_Cylindrical_r_perp_Scan.pdf}
    \caption{Giant connected component (GCC) diagnostic for the cylindrical
    scan. Top: fraction of galaxies in the GCC as a function of the transverse
    linking length $r_\perp$. Bottom: its derivative
    $\mathrm{d}\,\mathrm{GCC}/\mathrm{d}r_\perp$. The susceptibility peak
    ($r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc) and the maximum-gradient point
    ($1.36\,h^{-1}$ Mpc) locate the percolation threshold, while the
    fragmentation maximum ($r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc) marks the
    sub-critical group-finding scale adopted in this work.}
    \label{fig:gcc}
\end{figure}

\subsection{Linking parameter}

To connect the linking length derived from the network to the conventional FoF
literature, we express it as the dimensionless linking parameter
\begin{equation}\label{eq:b}
    b \equiv \frac{r_{\mathrm{groups}}}{\bar d},
    \qquad \bar d = n^{-1/3},
    \qquad n = \frac{N}{V},
\end{equation}
where $\bar d$ is the mean inter-galaxy separation and $n$ is the number density
of the sample. The volume-limited slice is a spherical shell between the comoving
distances at $z_{\min}$ and $z_{\max}$, restricted to the survey footprint,
\begin{equation}\label{eq:vol}
    V = \frac{4\pi}{3}\bigl(d_{\max}^3 - d_{\min}^3\bigr)\,f_{\mathrm{sky}},
    \qquad
    f_{\mathrm{sky}} = \frac{7221\,\mathrm{deg}^2}{41253\,\mathrm{deg}^2},
\end{equation}
the SDSS Main Galaxy Sample covers approximately $7221\,\mathrm{deg}^2$. This
approximates the survey volume as a uniform spherical shell. This gives
$b = 0.144$, near the lower boundary of the classical calibrated range
$0.14 < b < 0.20$ \cite{davis1985, berlind2002} and slightly above the
redshift-dependent Tempel et al.\ value ($b \approx 0.113$--$0.115$ over the
same redshift interval), which means Tempel et al.\ groups are bound tighter.
Again, the computation of $b$ is purely for comparison with the literature.

\subsection{Final network}

With $r_{\mathrm{groups}}$ fixed, the group catalogue is produced by building
the graph once at linking length $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc. The
resulting network consists of $N = 75{,}042$ nodes and $95{,}310$ edges. Its
connected components are extracted and each galaxy is assigned a group label.
The output is the most basic way to construct a network, two galaxies are members
of the same group if they are connected by a chain of edges. This method has no
resolution limit (even the smallest groups are detected), but the price is paid
with sensitivity to bridging, where a single close pair can merge two separate
systems. Both we will discuss later in Discussion.

\section{Results}

\subsection{The percolation transition}

\figref{fig:phase} shows both observables across the full scan. The
susceptibility peak at $r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc and the
fragmentation maximum at $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc are both
consistent with the two scales located in \figref{fig:gcc}.

This transition is the spatial graph analogue of the giant component transition
in random graph theory. In the case of an Erdhos Renyi graph, the giant
component appears when the mean degree $\langle k\rangle = 1$. This corresponds
to an edge probability $p_c = 1/N$.

In a random geometric graph, the giant component appears at a constant critical
mean degree, which is known as continuum percolation. This threshold does not
depend on $N$. In our case, the mean degree is $\langle k\rangle = 2.54$. This
value is much higher than the threshold where a giant component would form in a
random graph. However, in the FoF network, we do not observe a similar giant
component. Instead, groups form locally before global percolation takes place.
The mean-field estimate below gives a quantitative comparison for this
difference.

The sharpness of the peak is a finite-size effect. A true divergence occurs only
in the thermodynamic limit. For a finite graph, the transition is a smooth
crossover whose width narrows as $N$ grows. The volume-limited sample has
$\sim 7.5\times10^4$ galaxies, drawn from a parent catalogue (MGS) of
$\sim 6\times10^5$. This is still large enough for the peak to be well
localised, so $r_{\mathrm{critical}}$ can be found with good precision. The
close agreement between the susceptibility peak and the maximum-gradient point
also confirms this.

The mean-field theory of percolation offers a method to check these numbers
using analytical techniques. In an Erdhos Renyi graph, the probability
$u$ of a randomly chosen node being outside the giant component is given by the
self-consistency equation
\begin{equation}\label{eq:gcc_mf}
    u = e^{\lambda(u-1)},
\end{equation}
where $\lambda = \langle k\rangle$. A nontrivial solution ($u < 1$, giant
component present) exists only for $\lambda > 1$, which yields the mean-field
threshold at $\langle k\rangle = 1$. At the operating point of this work,
$\lambda = 2.54$, the equation gives $u \approx 0.10$. A random graph with the
same edge density would place $90\%$ of its nodes in a single giant component,
more than a hundred times bigger than the $0.8\%$ observed in our FoF network.
The way galaxies are arranged in the cosmic web prevents the global percolation
that mean-field theory predicts at this average degree. This means the galaxy
distribution is too uneven for a large connected structure to form at the scale
where groups appear.

\begin{figure}[t]
    \centering
    \placefig{figures/Anisotropic_Phase_Transition_Cylindrical_r_perp.pdf}
    \caption{The anisotropic percolation transition. The susceptibility
    $\chi(r_\perp)$ (left axis) and the number of finite groups
    $N_{\mathrm{groups}}(r_\perp)$ (right axis) are shown against the transverse
    linking length. $\chi$ peaks at the percolation threshold
    $r_{\mathrm{critical}} = 1.34\,h^{-1}$ Mpc (dashed), while the fragmentation
    count peaks at the sub-critical group scale $r_{\mathrm{groups}} =
    0.54\,h^{-1}$ Mpc (dotted), the operating point of the group finder.}
    \label{fig:phase}
\end{figure}

\subsection{Group catalogue: sky projection}

Before performing any quantitative validation, we can check the group catalogue
qualitatively. \figref{fig:radec} projects the Tempel et al.\ catalogue (left
column) and the detected catalogue (right column) onto the plane of the sky in
four redshift bins of width $\Delta z = 0.01$ (rows). Grouped galaxies are
coloured by their group label and sized by richness. Field galaxies appear in
faint grey. Even though this projection is qualitative and collapses the line
of sight, the two catalogues look similar. Both dense patches and empty voids of
the sky show up similarly in both columns.

Collapsing the line of sight is costly. The projection cannot tell whether two
galaxies that look close on the sky really belong to the same group in three
dimensions. That test is left to the quantitative metrics in
\S\ref{sec:validation}.

The panel by panel agreement also gives us a free check on the coordinate
transformation. To draw the map, each galaxy must be moved from spherical survey
coordinates to a 3D position. A mistake in the coordinate transformation would
place our groups at slightly wrong positions. The groups line up between the two
columns, so the coordinate code is working.

A second thing to check is what happens at the bin edges. Large structures are
bigger than one redshift slice, so the same structure can appear in two stacked
rows. A naive finder might cut such a structure in half at the boundary. This
does not happen here. In both catalogues these extended overdensities stay whole
and are assigned to the same structures, so the cylindrical FoF follows them
across adjacent bins instead of breaking them at the panel edges.

However, having a closer look at the panels, one might notice that the detected
groups are slightly more extended than the Tempel groups. This is a sign of
over-merging, whose causes we discuss quantitatively later in
\S\ref{sec:discussion}.

\begin{figure*}[p]
    \centering
    \placefig[width=\textwidth]{figures/Galaxy_distribution_and_groups_in_RA_Dec_detected_vs_Tempel_2014_per_redshift_bin.pdf}
    \caption{Sky projection (RA--Dec) of the Tempel et al.\ catalogue (left
    column) and the detected groups (right column), in four redshift bins of
    width $\Delta z = 0.01$ (rows). Grouped galaxies are coloured by group
    membership and sized by richness; field galaxies are grey.}
    \label{fig:radec}
\end{figure*}

\subsection{Group richness distribution}

If the linking length is to be appropriate, the two catalogues should also agree
in their group-size statistics. \figref{fig:richness} compares the richness
distributions (the number of members per group) of the detected and Tempel
catalogues, with groups of at least two members. The two distributions share the
same steep shape. They do not match exactly, though: the detected catalogue lies
slightly above Tempel across the whole range, and the excess grows at high
richness, where it also reaches a marginally heavier tail. This small excess at
high $N$ is the histogram-level trace of the over-merging measured later in
\S\ref{sec:validation}, not a shift toward lower richness.

\begin{figure}[t]
    \centering
    \placefig{figures/Group_Richness_Distribution.pdf}
    \caption{Group richness distribution (members per group, $N \ge 2$) for the
    detected catalogue (filled) and the Tempel et al.\ catalogue (step). The two
    share the same steep shape, but the detected catalogue lies slightly above
    Tempel and carries a marginally heavier high-$N$ tail, the signature of mild
    over-merging (an excess at low $N$ would instead signal fragmentation).}
    \label{fig:richness}
\end{figure}

\section{Validation \& Assessment}\label{sec:validation}

Validating the detected groups catalogue against Tempel et al.\ is a partition 
comparison task; both catalogues partition the same set of galaxies into gravitationally bound groups, and
so the question will be how closely these two partitions agree. We answer this question at three
levels: 1) individual matched groups (by richness and by redshift), 2) the global
partition, and the 3) network topology (clustering and degree distribution).

\subsection{Completeness and purity by richness}
For each Tempel group $T$ (with at least two members) we
identify the detected group $D$ which shares the most galaxies with $T$ and
compute
\begin{equation}\label{eq:cp}
    \mathrm{Completeness} = \frac{|T \cap D|}{|T|}, \qquad
    \mathrm{Purity} = \frac{|T \cap D|}{|D|}.
\end{equation}
Completeness is the fraction of a reference group ($T$) that is
recovered by a group $D$ from the detected catalogue, while purity is the
fraction of the matched detected group that is genuine.
In other words, completeness measures how many of the actual members of a Tempel group
are recovered, while purity measures how many of
the recovered members are actually true members of that Tempel group. A group
with high completeness but low purity contains all the
true members but also a large number of alien members. A group with high purity
but low completeness contains only true members, but misses many of them.

Binned by richness (\tabref{tab:richness}), completeness is uniformly high
($\gtrsim 0.98$) and shows no trend meaning that essentially every member of a Tempel group
is recovered. Completeness is exactly $1$ for $97.5\%$ of matched groups; the
small fragmenting tail ($2.5\%$, completeness as low as $0.5$) is what pulls
the bin averages below unity. Purity, on the other hand, declines monotonically, from $0.82$ for pairs to
$0.75$ for the crowded groups. The detected groups thus contain almost all the true
members, but in the richest systems a considerable number of additional galaxies are also
swept inside. Taking the total number of these extra galaxies across all detected groups, only about $12\%$
of them are galaxies that are ungrouped in the Tempel catalogue (field galaxies); the remaining $88\%$ belong to
another Tempel group entirely, and this other-group share itself rises with richness (\tabref{tab:richness}),
reaching about $74\%$ in the richest bin. Over-merging is therefore mostly whole neighbouring groups being
bridged into one detected structure, not scattered field galaxies leaking in.
Rich groups sit in the densest environments, where it is easier for a single fixed linking length to
bridge to neighbouring galaxies and structures. This decrease in purity reflects
the bridging effect at higher densities and is not related to internal velocity dispersion of the groups as it would
also affect the completeness.

\begin{table}[H]
    \caption{Mean completeness and purity of matched groups, binned by Tempel
    group richness $N_T$, together with the fraction of the purity loss that
    comes from another Tempel group rather than a field galaxy.}
    \label{tab:richness}
    \centering
    \begin{tabular}{lccc}
        \toprule
        Richness $N_T$ & Completeness & Purity & Other-group share \\
        \midrule
        $2$          & 0.995 & 0.824 & 0.556 \\
        $3$--$5$     & 0.992 & 0.805 & 0.554 \\
        $6$--$10$    & 0.984 & 0.778 & 0.595 \\
        $11$--$50$   & 0.984 & 0.757 & 0.609 \\
        $51$--$500$  & 0.989 & 0.753 & 0.742 \\
        \bottomrule
    \end{tabular}
    \tabletext{Means are unweighted averages over all Tempel groups in each
    richness bin. The bins are defined by Tempel group size $N_T$; the
    declining purity with richness reflects over-merging in the most massive
    systems. The other-group share is the fraction of each matched group's
    non-member galaxies ($D \setminus T$) that belong to a different Tempel
    group rather than being Tempel-field; it also rises with richness.}
\end{table}

\subsection{Completeness and purity by redshift}

\figref{fig:cpz} repeats the analysis for matched groups binned by mean group
redshift. Completeness remains nearly constant at unity across the whole range (consistent
with the global value of $0.98$ in \tabref{tab:metrics}) and purity stays close to
its mean of $\approx 0.79$ but moves slightly upward with redshift, from
$\approx 0.78$ in the lower redshift bins to $\approx 0.85$ in the highest. This can be traced back to the
bridging effect discussed above. Purity is related to local galaxy density, not richness, as
richness itself shows no trend with redshift. The denser a region of the space is, the more likely it is
that bridges form that connect two distinct groups. Within the volume-limited sample, the galaxy number density is
highest near $z_{\min}$ and drops by roughly $40\%$ toward $z_{\max}$. This is
most likely due to real large scale structures rather than a selection effect, since
$M_{\mathrm{lim}}$ already removes the redshift dependence of the flux limit.
Completeness and purity are sensitive to different failure modes.
Completeness falls when true members of $T$ end up outside $D$, as a result of
under-linking. Adding non-member galaxies to an already complete
group still leaves completeness at $1$, so over-linking affects purity but
cannot lower completeness. In the connected-components clustering method, having more edges
results only in merging components together, never splitting them, so this asymmetry is
structural, not incidental. A constant, near-unity completeness across
redshift therefore shows only that under-linking is not the issue here.
The fixed comoving $r_\perp$ gathers essentially all true members
inside the volume-limited range, consistent with the sky
projection of \figref{fig:radec}.

\begin{figure}[t]
    \centering
    \placefig{figures/Completeness_and_Purity_by_Redshift_Bin.pdf}
    \caption{Mean completeness and purity of matched groups as a function of
    mean group redshift. Completeness is flat near unity and purity stays around
    its mean with a mild rise toward higher redshift, so the fixed transverse
    linking length remains appropriate at all distances.}
    \label{fig:cpz}
\end{figure}

\subsection{Global partition metrics}
The basis for the global partition comparison is the 41,430 galaxies grouped by both catalogues into
groups of at least 2 members. This is 10,907 Tempel groups and 9,739 detected groups.
The remaining (75,042 - 41,430 = 33,612) galaxies are singletons that Tempel classifies
as field galaxies. This is not a homogeneous set. Some of its galaxies are also left
isolated by our method (genuine agreement), while others are linked into one of the
detected groups (a false positive the metrics below cannot see). Restricting the comparison
to the Tempel-grouped galaxies therefore doesn't affect completeness, since completeness was never
about field galaxies to begin with, but may slightly overstate
pair precision, since field galaxies that are linked into a detected group fall outside the
Tempel-grouped set, so they slip past the metrics unnoticed.
\tabref{tab:metrics} reports two standard partition-comparison
scores. The Adjusted Rand Index (ARI) measures the fraction of pairs which
both partitions agree upon, either by placing them in the same group (True positives) or in different
groups (True negatives).
Ranging [-1, 1], an ARI of 1 indicates perfect agreement (identical groups), 0
indicates a totally random grouping and -1 indicates absolute disagreement as
if they are intentionally set to have no overlapping. The Normalized Mutual
Information (NMI) measures how much one partition tells about the other. Scaling
[0, 1], an NMI of 1 means the two partitions are identical, while 0 means they are
completely independent. The detected partition scores ARI $= 0.82$ and NMI $= 0.98$.

Pair recall and pair precision derived from the pair confusion matrix make the
asymmetry even more apparent. Pair recall asks: out of all the pairs of galaxies
that belong in the same group in the Tempel catalogue, what percentage does our
algorithm successfully find? This is $0.98$, which means our catalogue
is highly complete. Pair precision, on the other hand, asks: of all the galaxy pairs our
algorithm claims were in the same group, what percentage are actually together in the Tempel
catalogue. For this work it is $0.71$. The other 0.29 are false positives.
This repeats the richness analysis conclusion stated above: the catalogue is highly
complete but also over-merged in the richest systems. To distinguish between our results and 
a random coincidence, we permute the detected labels 1000 times and recompute the ARI. This
gives a null distribution centred at zero with a standard deviation of $5.7\times10^{-5}$. The
observed value lies far outside of it ($z \approx 1.4\times10^4$), which confirms the agreement
is not produced by chance.

\begin{table}[H]
    \caption{Global partition-comparison metrics between the detected and Tempel
    catalogues, on the grouped subsample ($41{,}430$ galaxies).}
    \label{tab:metrics}
    \centering
    \begin{tabular}{lc}
        \toprule
        Metric & Value \\
        \midrule
        Adjusted Rand Index (ARI)          & 0.824 \\
        Normalized Mutual Information (NMI) & 0.983 \\
        \midrule
        Pair precision                     & 0.710 \\
        Pair recall                        & 0.982 \\
        Pair $F_1$                         & 0.824 \\
        \bottomrule
    \end{tabular}
    \tabletext{ARI and pair $F_1$ coincide numerically to three digits; they
    measure distinct properties of the partition and the agreement is incidental.}
\end{table}

\subsection{Network topology}

The full network constructed with the maximum fragmentation criterion ($r_{\mathrm{groups}}$)
has 75,042 nodes and 95,310 edges; the network fragments into 37,500 connected components.
The largest group spans $585$ nodes ($0.8\%$ of the total galaxy population), consistent
with the sub-critical regime set by the susceptibility peak. The
mean degree is $\langle k\rangle = 2.54$. We consider an Erdhos Renyi null,
a random graph with the same number of nodes and edges, with the edge probability of
$p_{\mathrm{ER}} = 2E/[N(N-1)] = 3.4\times10^{-5}$. Its expected clustering
coefficient is $p_{\mathrm{ER}}$. The measured transitivity, $0.646$
(\tabref{tab:clustering}), exceeds this value by four orders of magnitude. While that
gap is real, it is weak on its own. Any graph built on spatial proximity
is locally clustered, so even if no cosmology is involved, it will beat any structureless
null in this regard.

A sharper test is a geometric null. We build a fake catalogue with the same 75,042 points,
scattered randomly. Their positions on the sky match the real footprint of the survey,
not the full sky, and their comoving distances are drawn so the number density stays constant
with depth.
These points are linked by the same cylindrical criterion we introduced before, with the
linking length selected using bisection methods to match the actual mean degree
$\langle k\rangle = 2.54$. The process is repeated 9 more times (10 in total) to avoid
random fluctuations. Averaging over these 10 iterations, this null has transitivity $0.45 \pm 0.002$
and mean local clustering $0.32$ (\tabref{tab:clustering}). The actual galaxy network matches
the null in mean local clustering ($0.32$) but exceeds it in transitivity ($0.646$ against
$0.45$, by a factor of $1.4$, far outside the $\pm 0.002$ error limit).
This excess is confined to the high-degree, dense environments that dominate
transitivity, namely the richest groups and cluster cores.
Gravitational clustering gathers galaxies into extra closed triangles that cannot
be produced by a uniform null. The same clustering is observed in the linking lengths. The
real network reaches $\langle k\rangle = 2.54$ at $0.54\,h^{-1}$ Mpc, which is less
than half the $\approx 1.3\,h^{-1}$ Mpc required by the uniform null to reach the same
mean degree.

\begin{table}[H]
    \caption{Clustering of the FoF network against an Erdhos Renyi null
    and a matched-density geometric null.}
    \label{tab:clustering}
    \centering
    \begin{tabular}{lccc}
        \toprule
        Quantity & FoF network & ER null & Geometric null \\
        \midrule
        Transitivity          & $0.646$ & $3.4\times10^{-5}$ & $0.45$ \\
        Mean local clustering & $0.322$ & $3.4\times10^{-5}$ & $0.32$ \\
        \bottomrule
    \end{tabular}
    \tabletext{The geometric null places $75{,}042$ points uniformly within the
    survey footprint and volume, linked at matched mean degree $\langle k\rangle
    = 2.54$; values are means over ten independent realisations ($\pm 0.002$
    scatter).}
\end{table}

\subsection{Degree distribution}

\figref{fig:degree} shows the degree distribution $P(k)$ of the same network on
semi-logarithmic and double-logarithmic axes, with the Poisson law of the
Erdhos Renyi null at the same mean degree overlaid. The distribution
departs from the null at both extremes. The isolated-node fraction is $34.9\%$
(field galaxies of degree zero), against a Poisson expectation of $7.9\%$, an
excess of $4.4\times$. At the opposite end a heavy tail reaches $k_{\max} = 43$,
populated by hub galaxies whose neighbourhoods a Poisson graph of the same mean
degree would essentially never produce. At high $k$ the null lies tens of orders
of magnitude below the data. Because probability is conserved, the
over-population of both extremes leaves the distribution depleted near its mean:
galaxies are either isolated in the field or embedded in a dense group, with few
in between. The hubs are the most connected, central members of the richest
groups.

The double-logarithmic panel shows the tail is not a pure power law: it is
concave and truncated at $k_{\max}$. The cutoff arises because the degree is
bounded by how many galaxies can occupy a single linking cylinder. Finite sample
size and luminosity selection contribute too, but at the linking length used here
the geometric constraint dominates. The network is heavy-tailed but not
scale-free: the linking cylinder's finite volume caps the maximum degree, not
preferential attachment.

\begin{figure*}[t]
    \centering
    \placefig[width=\textwidth]{figures/Degree_Distribution.pdf}
    \caption{Degree distribution $P(k)$ of the FoF network on semi-logarithmic
    (left) and double-logarithmic (right) axes. The dashed curve is the Poisson
    law of an Erdhos Renyi graph of the same mean degree. The data exceed
    the null at both ends (a $34.9\%$ isolated fraction and a heavy tail to
    $k_{\max} = 43$) and the tail is concave and truncated, not a pure power
    law.}
    \label{fig:degree}
\end{figure*}

\subsection{Internal structure of the richest groups}

The global metrics in \S\ref{sec:validation} are computed on the $9{,}739$
detected groups; \tabref{tab:groups} profiles the eight richest of these
individually. The eight groups show a consistent pattern: the internal edge
density rises as groups shrink, from $0.02$ for the largest to $0.08$ for the
smallest tabulated. The transitivity stays in the range $0.54$--$0.64$
throughout. Yet even the richest groups are far from compact. Diameters of $16$
to $36$ links across only a few hundred members point to elongated, sparsely
connected structures, not clique-like balls. Single-linkage clustering produces
exactly this, a direct manifestation of its bridging sensitivity. The giant
connected component ($585$ members, diameter $22$) is the largest such chain;
group $1017$, with a diameter of $36$ across $226$ members, is the most
filamentary of all.

The same bridging sensitivity drives the over-merging in \tabref{tab:richness}
and \tabref{tab:metrics}.

\begin{table}[H]
    \caption{Internal structure of the eight richest detected groups: number of
    members $N$, internal edge density, transitivity, and diameter.}
    \label{tab:groups}
    \centering
    \begin{tabular}{rrccc}
        \toprule
        Group & $N$ & Density & Transitivity & Diameter \\
        \midrule
        133  & 585 & 0.021 & 0.561 & 22 \\
        4    & 302 & 0.038 & 0.542 & 17 \\
        0    & 301 & 0.047 & 0.579 & 18 \\
        1017 & 226 & 0.040 & 0.615 & 36 \\
        242  & 156 & 0.046 & 0.635 & 19 \\
        262  & 154 & 0.064 & 0.594 & 17 \\
        251  & 142 & 0.070 & 0.612 & 16 \\
        10   & 141 & 0.084 & 0.629 & 16 \\
        \bottomrule
    \end{tabular}
    \tabletext{Group $133$ is the giant connected component.}
\end{table}

\section{Discussion}\label{sec:discussion}

The fragmentation criterion selects $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc
($b = 0.144$) from the network alone, with no reference catalogue, and the
resulting groups reproduce the Tempel partition at ARI $= 0.82$. The
topologically chosen linking length is slightly larger than Tempel's effective
$b \approx 0.113$--$0.115$, and the validation lines up with that. A marginally
longer linking length explains the over-merging we see: declining purity in rich
systems (\tabref{tab:richness}) and pair precision below recall
(\tabref{tab:metrics}). The fragmentation maximum favours linking lengths just
large enough to assemble groups fully, at the cost of occasionally bridging
close pairs, the characteristic weakness of single linkage.

The percolation scan also converts a free parameter into a measured one. The
susceptibility and fragmentation curves give two physically motivated scales:
$r_{\mathrm{critical}}$, above which group finding recovers filaments rather
than groups, and $r_{\mathrm{groups}}$, at which the group population is
largest. The sub-critical inequality $r_{\mathrm{groups}} <
r_{\mathrm{critical}}$ is an internal consistency check that any FoF catalogue
should satisfy.

Single-linkage clustering has no resolution limit but is sensitive to bridging:
a single close pair merges two groups and produces the extended, filamentary
components of \tabref{tab:groups} and the over-merging in rich systems. The
fixed stretch factor $S = 10$ cannot adapt to the full range of cluster velocity
dispersions either. We have not scanned $S$, so its separate contribution to
the over-merging is not isolated here. A single transverse linking length is
held across the full volume. The redshift-binned metrics show this is adequate
over $0.02 \le z \le 0.06$, but a deeper sample would need a
redshift-dependent length. The survey footprint also truncates groups at its
boundary, an edge effect not corrected here.

More broadly, $b = 0.144$ lands near the lower edge of the classical calibrated
range $0.14$--$0.20$ \cite{davis1985, berlind2002}, and it gets there with no
external calibration input. That hints the percolation transition tracks the
physical group-assembly scale rather than an artefact of how the scan was set up.

\section{Conclusion}

The fragmentation maximum at $r_{\mathrm{groups}} = 0.54\,h^{-1}$ Mpc, read
from a percolation scan with no reference to any external catalogue, reproduces
the Tempel et al.\ partition at ARI $= 0.82$ and NMI $= 0.98$, showing that
network topology alone fixes the group-finding scale. The residual disagreement
is over-merging in the richest systems, attributable to single-linkage bridging
and the fixed stretch factor rather than to a problem with the percolation
criterion. That $b = 0.144$ falls near the lower edge of the classical
calibrated range without being calibrated there suggests the percolation
criterion tracks the physical group-assembly scale.

%----------------------------------------------------------

\printbibliography

%----------------------------------------------------------

\end{document}
